# The canonical environment template lives at the repository root.
# See ../.env.example
#
# This file is kept as a pointer so that older tooling that looks under
# backend/ for an example file still finds something useful. To configure
# the backend, copy `../.env.example` to `../.env` (the dotenv loader in
# `app/main.py` walks up the directory tree to find it).
