fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
httpx>=0.27.0
pydantic>=2.7.0

# LangGraph / LangChain
langgraph>=0.2.50
langchain>=0.3.0          # required by langfuse v3's LangChain CallbackHandler
langchain-core>=0.3.0

# Google Gemini chat models
langchain-google-genai>=2.0.0

# RAG / OpenSearch (optional knowledge-base backend; in-memory demo ships by default)
opensearch-py>=2.4.0

# Observability — Langfuse self-host or cloud (LangChain callback integration).
langfuse>=3.0.0,<4
